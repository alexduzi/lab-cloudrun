version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lab-cloudrun-api
    ports:
      - "${APP_PORT:-8080}:${APP_PORT:-8080}"
    environment:
      # Required
      - WEATHER_API_KEY=${WEATHER_API_KEY}

      # Application Configuration
      - APP_PORT=${APP_PORT:-8080}
      - GIN_MODE=${GIN_MODE:-release}

      # Optional - API Base URLs (uses defaults if not set)
      - VIA_CEP_BASE_URL=${VIA_CEP_BASE_URL:-https://viacep.com.br/ws/{cep}/json/}
      - WEATHER_BASE_URL=${WEATHER_BASE_URL:-http://api.weatherapi.com/v1/current.json}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:${APP_PORT:-8080}/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
